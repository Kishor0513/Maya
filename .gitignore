node_modules/
dist/
.env
server/.env
*.log

# gateway data (sqlite + JSON fallback hold real user content)
data/

# test artifacts
test-results/
playwright-report/

# tsc build artifacts (composite node config emits these next to source)
vite.config.js
vite.config.d.ts

# tsc incremental state — never commit, it goes stale on other machines
*.tsbuildinfo
